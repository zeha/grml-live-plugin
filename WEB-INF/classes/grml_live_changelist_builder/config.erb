<%
f = taglib("/lib/form")
f.block do
  f.entry :title => "Output filename",
          :field => "output_filename",
          :description => 'Defaults to "changelog.txt".' do
    f.textbox
  end
  f.entry :title => "dpkg.list.old name",
          :field => "dpkg_list_old_name",
          :description => 'dpkg.list to compare against. Defaults to "dpkg.list.old".' do
    f.textbox
  end
  f.entry :title => "Package Prefix",
          :field => "package_prefix",
          :description => 'No default. Example: "grml".' do
    f.textbox
  end
  f.entry :title => "Base Git URL",
          :field => "git_url_base",
          :description => 'No default. Example: "git://github.com/grml".' do
    f.textbox
  end
end
%>
