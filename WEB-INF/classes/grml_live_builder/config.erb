<%
f = taglib("/lib/form")
f.block do
  f.entry :title => "Product Name",
          :field => "name",
          :description => 'Defaults to "autobuild".' do
    f.textbox
  end
  f.entry :title => "Release Codename",
          :field => "codename",
          :description => 'Defaults to "autobuild-VERSION".' do
    f.textbox
  end
  f.entry :title => "Debian Suite", :field => "suite" do
    f.textbox
  end
  f.entry :title => "Architecture", :field => "arch" do
    f.textbox
  end
  f.entry :title => "Classes", :field => "classes" do
    f.textbox
  end
  f.entry :title => "Build Directory",
          :field => "output_directory",
          :description => 'Build Directory defaults to workspace if unset.' do
    f.textbox
  end
  f.entry :title => "Version",
          :field => "version",
          :description => 'If you want to use an autogenerated version string enable the setting "Use date as version".' do
    f.textbox
  end
  f.entry :title => "Extract previous ISO",
          :field => "extract_iso",
          :description => "If you want to update or build from an existing ISO, specify its name here." do
    f.textbox
  end
  f.entry :title => "Use date as version (YYYYMMDD)", :field => "version_from_date" do
    f.checkbox
  end
  f.entry :title => "Do not update chroot",
          :field => "build_only",
          :description => "(build-only mode)" do
    f.checkbox
  end
end
%>
